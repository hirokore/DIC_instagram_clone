<div class="users_h1_box"><h1><%= @users.name %>のページ</h1><%= link_to "アカウント編集", edit_user_path%></div>
<p id="notice"><%= notice %></p>
<p>
  <strong>メールアドレス：<%= @users.email %>
</p>

<% if @users.image.url  %>
  <p>
    <strong>イメージ:</strong>
    <%= image_tag @users.image.url %>
  </p>
<% end %>
